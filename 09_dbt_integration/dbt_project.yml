name: onboarding_analytics
version: 1.0.0
config-version: 2

profile: onboarding_analytics

model-paths:
  - models
test-paths:
  - tests
clean-targets:
  - target
  - dbt_packages

models:
  onboarding_analytics:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
